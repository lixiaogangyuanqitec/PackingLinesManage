<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yuanqi.packinglines.inter.LineOperation">
    
    <!-- 查询包装线的线体信息 -->
    <select id="selectPackLineInfo" parameterType="String" resultType="Line">
        select * from t_thread where roomid=#{roomId} 
    </select>
</mapper>

	